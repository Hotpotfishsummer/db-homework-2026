# API 配置
# 复制此文件为 .env.local 并填写实际值

# 前端访问后端 API 的基础路径（Vite dev server 会代理到 backend 容器）
VITE_API_BASE_URL=/api/v1

# Vite dev server 的后端代理目标（compose 内通常是 backend 服务名）
VITE_BACKEND_PROXY_TARGET=http://localhost:8000
